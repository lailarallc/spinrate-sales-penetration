# Environment and secrets
.env
.env.*
!.env.example
*.key
credentials.*

# Node
node_modules/
dist/
build/
.cache/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# Data files (raw data stays local)
data/raw/
*.csv
*.xlsx
*.xls
!data/synthetic/*.csv

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/

# Logs
*.log

# Security - secret leak prevention
*.pem
secrets.*

# Dev artifacts (local UI review — not app history)
review.yaml
screenshots/

# Local-only audit notes (for my eyes, not repo history)
docs/AUDIT-*.md

# --- lailara engagement scaffold ---
# Client engagement data is runtime-only: never commit it, never deploy it.
client-data/
client-output/
/engagement.yml
/engagement.yaml
# (engagement.demo.yml and engagement.example.yml stay committable)
